import { CaseUpperPipe } from './case-upper.pipe';

describe('CaseUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new CaseUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
